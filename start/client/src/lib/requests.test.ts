import { getFoods, postFood } from "./requests";
import { fruits } from "../mocks/fruits";
describe("requests with mocking fetch", () => {
  it.todo("should make a GET request to a mocked API");
  it.todo("should make a POST request to a mocked API");
  it.todo("should throw an error if response of GET is not okay");
  it.todo("should throw an error if response of POST is not okay");
});
